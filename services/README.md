# Services Package

Core service implementations currently run from:

- `backend/app/services`

This placeholder folder is reserved for shared libraries or independently deployable service modules as the platform scales.
